# 多 LLM 智能分工架构

> DeepSeek / Qwen / Doubao 三大模型的协作分工策略

## 1. 设计理念

写作是一个多维度的创作过程，不同阶段需要不同的能力：
- **规划阶段**需要全局把控能力
- **逻辑阶段**需要推理和结构化思考
- **创作阶段**需要文学创意和优美文笔

单一大模型难以在所有维度都达到最优，因此我们采用**多 LLM 协作架构**。

## 2. 三大模型特性

| 模型 | 提供商 | 核心优势 | 适合任务 | 相对成本 |
|------|--------|----------|----------|----------|
| **Qwen** | Aliyun | 长上下文窗口、全局记忆强 | 总览规划、设定管理 | 中等 |
| **DeepSeek** | DeepSeek | 逻辑推理强、结构化思考 | 事件设计、质量评估 | **低** |
| **Doubao** | 火山引擎 | 文学创作强、文笔流畅 | 内容生成、润色修订 | 中等 |

## 3. 任务路由架构

```
┌─────────────────────────────────────────────────────────────────┐
│                    Task Router (任务路由器)                      │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐    │
│  │                 task_type_map 路由表                    │    │
│  │                                                        │    │
│  │  大纲 ──────────────▶ Qwen (0)                        │    │
│  │  风格元素 ──────────▶ Qwen (0)                        │    │
│  │  人物设计 ──────────▶ Qwen (0)                        │    │
│  │  主题确认 ──────────▶ Qwen (0)                        │    │
│  │  世界观规则 ────────▶ Qwen (0)                        │    │
│  │                                                        │    │
│  │  事件 ──────────────▶ DeepSeek (1)                    │    │
│  │  场景物品冲突 ──────▶ DeepSeek (1)                    │    │
│  │  评估 ──────────────▶ DeepSeek (1)                    │    │
│  │  一致性检查 ────────▶ DeepSeek (1)                    │    │
│  │  时间线检查 ────────▶ DeepSeek (1)                    │    │
│  │                                                        │    │
│  │  章节内容 ──────────▶ Doubao (2)                      │    │
│  │  修订 ──────────────▶ Doubao (2)                      │    │
│  │  润色 ──────────────▶ Doubao (2)                      │    │
│  │  对话检查 ──────────▶ Doubao (2)                      │    │
│  │                                                        │    │
│  └────────────────────────────────────────────────────────┘    │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
                              │
         ┌────────────────────┼────────────────────┐
         │                    │                    │
         ▼                    ▼                    ▼
┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐
│  🧠 Qwen        │  │  🔍 DeepSeek    │  │  ✨ Doubao      │
│  总览记忆专家    │  │  逻辑结构专家   │  │  创意文笔专家   │
├─────────────────┤  ├─────────────────┤  ├─────────────────┤
│ • 200K+ tokens  │  │ • 强逻辑推理    │  │ • 文学创作强    │
│ • 全局一致性    │  │ • 因果关系合理  │  │ • 文笔优美      │
│ • 长期记忆好    │  │ • 性价比高      │  │ • 对话生动      │
└─────────────────┘  └─────────────────┘  └─────────────────┘
```

## 4. 详细任务分工

### 4.1 🧠 Qwen - 总览记忆专家

**负责任务**：
- **大纲**：统筹全局，设定整体故事框架
- **风格元素**：定义风格特点，确保全书风格一致
- **人物设计**：设计人物关系网络，记住所有设定
- **主题确认**：确定主题方向
- **世界观规则**：构建世界设定

**选择原因**：
```
Qwen 的长上下文优势在"规划类"任务中至关重要：

1. 大纲需要同时考虑：
   • 故事开端、发展、高潮、结局的整体弧线
   • 多条支线的交织与收束
   • 主题的层层深化

2. 人物设计需要同时把控：
   • 主要人物 + 次要人物的完整关系网
   • 每个人物的背景、动机、性格、弧光
   • 人物间的冲突与羁绊

→ 这些都需要"全局视野"，Qwen 的长记忆是关键
```

### 4.2 🔍 DeepSeek - 逻辑结构专家

**负责任务**：
- **事件**：设计逻辑清晰的事件链
- **场景物品冲突**：构建合理的场景和冲突
- **评估**：客观分析内容质量
- **一致性检查**：检测逻辑漏洞
- **时间线检查**：验证时间线合理性

**选择原因**：
```
DeepSeek 的逻辑推理优势在"结构类"任务中表现突出：

1. 事件链设计需要：
   • A事件 → B事件 → C事件 的因果必然性
   • 悬念的合理铺垫与揭示
   • 时间线的准确把控

2. 评估任务需要：
   • 发现逻辑漏洞（如时间矛盾、人物行为不一致）
   • 量化打分的客观性
   • 提出具体可执行的改进建议

→ DeepSeek 的推理能力 + 高性价比，是逻辑任务的最佳选择
```

### 4.3 ✨ Doubao - 创意文笔专家

**负责任务**：
- **章节内容**：生成生动有趣的故事内容
- **修订**：润色文字，提升可读性
- **润色**：优化语言表达
- **对话检查**：确保对话自然流畅

**选择原因**：
```
Doubao 的文学创作优势在"内容类"任务中不可替代：

1. 章节内容创作需要：
   • 优美的叙事语言
   • 生动的场景描写
   • 自然的人物对话
   • 恰到好处的情感渲染

2. 修订润色需要：
   • 语感敏锐，识别不自然表达
   • 词汇丰富，避免重复枯燥
   • 节奏把控，张弛有度

→ Doubao 的文学素养，让创作内容更具可读性和艺术性
```

## 5. 执行流程中的模型切换

```
┌─────────────────────────────────────────────────────────────────────┐
│                        小说创作执行流程                              │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  阶段一：规划阶段 ───────────────────────────── 🧠 Qwen             │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  [风格] → [主题确认] → [市场定位] → [大纲] → [人物设计]      │   │
│  │                                                             │   │
│  │  Qwen 负责：确保整体框架完整、设定一致、无遗漏               │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                              │                                      │
│                              ▼                                      │
│  阶段二：结构阶段 ───────────────────────────── 🔍 DeepSeek         │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  [事件] → [场景物品冲突] → [伏笔列表] → [一致性检查]         │   │
│  │                                                             │   │
│  │  DeepSeek 负责：确保逻辑合理、因果清晰、无矛盾               │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                              │                                      │
│                              ▼                                      │
│  阶段三：创作阶段（循环） ─────────────────────── ✨ Doubao         │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  for chapter in chapters:                                   │   │
│  │      [章节大纲] ──▶ [场景生成] ──▶ [章节内容] ──▶ [润色]      │   │
│  │                           │                                 │   │
│  │                           ▼                                 │   │
│  │                    🔍 DeepSeek: [评估]                       │   │
│  │                           │                                 │   │
│  │                     通过? ─┼─ 否 ──▶ ✨ Doubao: [修订]       │   │
│  │                           │                                 │   │
│  │                          是                                  │   │
│  │                           ▼                                 │   │
│  │                     保存，下一章                             │   │
│  │                                                             │   │
│  │  Doubao 负责：文笔优美、对话生动、情感到位                   │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

## 6. 代码实现

### 6.1 MultiLLMClient

```python
class MultiLLMClient(LLMClientBase):
    """多 LLM 智能分工客户端"""
    
    def __init__(
        self,
        providers: List[LLMClientBase],
        strategy: str = "task_type_map",
        task_type_map: Optional[Dict[str, int]] = None,
    ):
        self.providers = providers
        self.strategy = strategy
        self.task_type_map = task_type_map or self._default_task_type_map()
    
    def _default_task_type_map(self) -> Dict[str, int]:
        return {
            # Qwen (0): 总览记忆专家
            "大纲": 0, "风格元素": 0, "人物设计": 0,
            "主题确认": 0, "市场定位": 0, "世界观规则": 0,
            
            # DeepSeek (1): 逻辑结构专家
            "事件": 1, "场景物品冲突": 1, "评估": 1,
            "一致性检查": 1, "时间线检查": 1, "伏笔列表": 1,
            
            # Doubao (2): 创意文笔专家
            "章节内容": 2, "修订": 2, "章节": 2,
            "场景": 2, "润色": 2, "对话检查": 2,
        }
    
    def _select_provider(self, task_type: Optional[str]) -> LLMClientBase:
        if task_type and task_type in self.task_type_map:
            index = self.task_type_map[task_type]
            return self.providers[index]
        return self.providers[2]  # 默认 Doubao
    
    async def generate(self, prompt: str, task_type: str = None, **kwargs):
        provider = self._select_provider(task_type)
        logger.info(f"Task '{task_type}' routed to {provider.name}")
        return await provider.generate(prompt, **kwargs)
```

### 6.2 配置使用

```python
# 创建多 LLM 客户端
providers = [
    AliyunLLMClient(),    # 0: Qwen
    DeepSeekLLMClient(),  # 1: DeepSeek
    DoubaoLLMClient(),    # 2: Doubao
]

llm_client = MultiLLMClient(
    providers=providers,
    strategy="task_type_map",
)

# 使用
response, usage = await llm_client.generate(
    prompt="请生成小说大纲",
    task_type="大纲"  # 自动路由到 Qwen
)
```

### 6.3 API 配置

```python
# src/creative_autogpt/api/server.py

def _create_llm_client(provider: Optional[str]) -> LLMClientBase:
    provider_name = (provider or "aliyun").lower()
    
    if provider_name == "multi":  # 智能分工模式
        providers = [
            AliyunLLMClient(),
            DeepSeekLLMClient(),
            DoubaoLLMClient(),
        ]
        return MultiLLMClient(providers=providers)
    
    # 单一模型模式
    if provider_name == "deepseek":
        return DeepSeekLLMClient()
    if provider_name == "doubao":
        return DoubaoLLMClient()
    return AliyunLLMClient()
```

## 7. 成本与质量优化

| 任务类型 | 模型 | 相对成本 | 质量提升 |
|---------|------|---------|---------|
| 大纲、设定 | Qwen | 中等 | ⬆️ 全局一致性 +30% |
| 逻辑推理 | DeepSeek | **最低** | ⬆️ 逻辑合理性 +25% |
| 内容创作 | Doubao | 中等 | ⬆️ 文学性 +40% |

**综合效果**：
- ✅ **质量更高**：各模型发挥专长
- ✅ **成本更优**：DeepSeek 处理大量逻辑任务，整体成本降低约 20%
- ✅ **可靠性强**：多模型互补，减少单点故障

## 8. 故障转移

```python
async def generate(self, prompt: str, task_type: str = None, **kwargs):
    primary = self._select_provider(task_type)
    fallback_order = self._get_fallback_order(primary)
    
    for provider in [primary] + fallback_order:
        try:
            return await provider.generate(prompt, **kwargs)
        except Exception as e:
            logger.warning(f"{provider.name} failed: {e}")
    
    raise AllProvidersFailedError()
```

---

*多 LLM 分工是提升创作质量和控制成本的关键策略。*
